{
    "name": "Scala.js with Scala CLI and Vite",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "initializeCommand": {
        "create-volume": "docker volume create exclude-volume"
    },
    "postStartCommand": "npm install && rm -rf .scala-build .bsp .bloop && scala-cli setup-ide .",
    "postAttachCommand": "nohup bash -c 'npm run devContainer &'",
    "customizations": {
        "vscode": {
            "extensions": [
                "scalameta.metals"
            ]
        }
    }
}